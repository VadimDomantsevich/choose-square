name: choose_square
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  bloc: ^8.1.4
  build_runner: ^2.4.13
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  freezed: ^2.5.7

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
